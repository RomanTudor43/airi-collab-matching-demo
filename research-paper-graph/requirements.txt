# Core
requests>=2.31
python-dotenv>=1.0
numpy>=1.24

# ML – embeddings (torch installed separately with CPU-only index in Dockerfile)
sentence-transformers>=2.2

# Community detection
networkx>=3.1
hdbscan>=0.8.33

# scikit-learn (used by sentence-transformers internally)
scikit-learn>=1.3
